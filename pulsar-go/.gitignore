bench-results/
.env
.env.*
*.log
perf.data.*
# Compiled example binaries (narrow paths: bare `server`/`stdlib` patterns
# would ignore the cmd/server and cmd/stdlib source directories themselves).
cmd/server/server
cmd/stdlib/stdlib
# Build outputs of the example binaries (rebuilt via `make static`).
/bin/
# NOTE: lib/libsolidc.a and lib/libpulsar.a are prebuilt musl archives and
# are intentionally tracked (see lib/.version). Only ignore transient
# rebuild byproducts.
/lib/*.o
/lib/*.tmp