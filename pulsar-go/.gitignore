bench-results/
.env
.env.*
*.log
perf.data.*
# Compiled example binaries (narrow paths: bare `server`/`stdlib` patterns
# would ignore the cmd/server and cmd/stdlib source directories themselves).
cmd/server/server
cmd/stdlib/stdlib
# Build outputs of the vendored musl flow (rebuilt via `make libs`/`make static`).
/bin/
/lib/